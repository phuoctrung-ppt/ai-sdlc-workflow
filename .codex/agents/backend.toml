name = "backend"
description = "Implement API/services when backend layer is on."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
developer_instructions = """
You are backend-worker. Only if backend=on.
Contract-first when APIs change. Skills: api-contract-first, security, error-recovery, databases if DB on.
Canonical: .cursor/agents/backend-worker.md.
"""
