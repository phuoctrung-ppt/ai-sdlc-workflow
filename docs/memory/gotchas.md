# Gotchas (Memory Layer)

> Patterns that already failed. Agents **must** check this file before inventing a solution.
> Updated by distillation after each module (Phase 6).
> Keep ≤ 10–15 active items. Move resolved ones to Archive.

## Active

<!-- Example:
- **tenant filter forgotten** — Every tenant-scoped query MUST include workspace_id / tenant_id.
  Seen in: module X, Y. Root cause: missing guard in repository base class.
-->

- No active gotchas yet.

## Archive

